# Factorio mod repository template

`src/` is the shipped mod source, including `src/changelog.txt`, which the
release workflow finalizes in place. Add the real
`LICENSE`, `COPYRIGHT`, `src/thumbnail.png`, and mod source before use.

`modPortalContent/` is reserved for future Mod Details and Mod Images API
automation and is not included in releases.
